"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function removeEmpty(obj, father) {
    for (let key in obj) {
        if (typeof obj[key] === 'object') {
            if (Object.keys(obj[key]).length === 0) {
                delete obj[key];
            }
            else {
                removeEmpty(obj[key], {
                    obj,
                    key,
                });
            }
        }
        else if (!obj[key]) {
            delete obj[key];
        }
    }
    if (Object.keys(obj).length === 0 && father) {
        delete father.obj[father.key];
    }
}
exports.default = {
    removeEmpty,
};
